<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In - Women Safety App</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        /* Global Styles */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        body {
            font-family: 'Open Sans', sans-serif;
            font-size: 16px;
            line-height: 1.5;
            color: #333;
            background: linear-gradient(to right, #c9e4ca, #87ceeb, #ffd7be);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        
        /* Container Styles */
        .container {
            max-width: 400px;
            padding: 30px;
            border-radius: 10px;
            background-color: rgba(255, 255, 255, 0.5);
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        
        /* Heading Styles */
        h1 {
            font-size: 36px;
            font-weight: 600;
            margin-bottom: 20px;
            color: #6c5ce7;
        }
        
        /* Paragraph Styles */
        p {
            margin-bottom: 20px;
            font-size: 18px;
            color: #555;
        }

        /* Input Field Styles */
        .input-field {
            margin-bottom: 20px;
        }
        .input-field input {
            width: 100%;
            padding: 15px;
            font-size: 18px;
            border: none;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .input-field input:focus {
            outline: none;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
        }

        /* Button Styles */
        .buttons {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .buttons button {
            width: 100%;
            padding: 15px;
            margin: 10px 0;
            background-color: #4CAF50;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .buttons button:hover {
            background-color: #3e8e41;
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                max-width: 300px;
            }
        }
        @media (max-width: 480px) {
            .container {
                max-width: 250px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Sign In</h1>
        <p>Welcome Back! Please sign in to continue.</p>
        <form method="post" action="/posts">
            <div class="input-field">
                <input type="email" placeholder="Email" required>
            </div>
            <div class="input-field">
                <input type="password" placeholder="Password" required>
            </div>
            <div class="buttons">
                <button>Sign In</button>
                <button>Sign Up</button>
            </div>
        </form>
    </div>
    <script src="signIn.js"></script> <!-- Ensure this points to the correct JS file -->
</body>
</html>